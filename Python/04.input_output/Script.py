# Input and output in Python

# ?output: we use print() function to display output on the screen

# a = "hello world"
# print(a)

# ? Formatted strings: we can use f-strings to format strings in Python

# name = "John"
# age = 30

# print(f"My name is {name} and I am {age} years old")

# ? Input: we use input() function to take input from the user
# age = input("Enter your age: ")
# print(f"You are {age} years old")

# *we have to manually convert the input to the required data type because input() function always returns a string
# Example:
# age = int(input("Enter your age: "))
# print(f"You are {age} years old")
